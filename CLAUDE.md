@AGENTS.md

## Headroom Session Snapshot
*Updated 2026-06-24 — retrieve full project state with:*
`mcp__headroom__headroom_retrieve({ hash: "74ddfb5cb3b8e211a55d9840" })`
Covers: security rules, fee model, PStTG, bugs fixed, CI status, go-live blockers, design tokens, key patterns.

**Session 2026-06-23 additions:** Full codebase audit complete. All Edge Functions clean. 3 additional fixes: cancel-contract now allows pending status; dead client-side cancelContract() removed; contracts.Row gained cancellation_reason field.

**Session 2026-06-24 additions:** 3 more fixes: pro.tsx fake Pro activation blocked (AsyncStorage beta removed, subscribe/cancel route to alerts until Stripe Billing live); BetaBanner C.green → C.primary; DsgvoConsent fontWeight '800' → '700'; GlobalAlert shadowColor '#000' → C.ink. TypeScript 0 errors throughout.

## Headroom Learned Patterns
*Auto-generated by `headroom learn` on 2026-06-21 — do not edit manually*

### Large Files
*~5,000 tokens/session saved*
- `werkr-prototype.html` is 289KB (3000+ lines) — exceeds the 256KB Read limit. Never attempt a full Read. Use `grep -n '^function ' werkr-prototype.html` to locate line numbers, then read with `offset` + `limit`. For screen audits, grep for function names first.
- `app/onboarding-kyc.tsx` is ~44KB — read with offset/limit when targeting specific sections.

### Database Schema
*~3,000 tokens/session saved*
- Schema spans 19+ migrations (`001_initial_schema.sql` through `019_provider_profile_missing_fields.sql`). No single file has all tables — grep across all: `grep -rn 'create table' supabase/migrations/`.
- `contracts` / `offers` tables are in `002b_contracts_offers_tables.sql` (NOT `001`). Stripe/escrow fields in later migrations. `pstTg` compliance columns in `012`.
- `jobs` table gained `address_plz`, `address_city`, `category` in `018_jobs_and_profiles_field_rename.sql`. `provider_profiles` got extra fields in `019`.

### Design System
*~3,000 tokens/session saved*

**Imports per screen:**
- Colors: `import { C } from '../../constants/colors'`
- Typography: `import { T } from '../../constants/typography'` ← NOT from theme.ts
- Shadows/spacing: `import { shadow, S, R } from '../../constants/theme'`

**Color tokens (C) — current accurate values:**
| Token | Value | Role |
|---|---|---|
| `C.bg` | `#F9F8F5` | Main canvas |
| `C.bgWarm` / `C.hair` | `#F2EFE9` | Insets, pressed |
| `C.surface` | `#FFFFFF` | Cards, sheets |
| `C.border` | `#E5E1DA` | Dividers |
| `C.ink` | `#1A1917` | Primary text |
| `C.sub` | `#6C6862` | Secondary text |
| `C.muted` | `#A8A49C` | Placeholder |
| `C.primary` | `#1B5C40` | Brand green — CTAs, active |
| `C.primaryBg` | `#EBF4EF` | Green tint |
| `C.gold` | `#8F6B1A` | Pro/premium accent |
| `C.goldBg` | `#F6ECD8` | Gold tint |
| `C.clay` | `#9B3E25` | Terrakotta accent |
| `C.red` | `#B91C1C` | Errors / destructive |
| `C.green` | – | **DEPRECATED** → `C.primary` |
| `C.greenBg` | – | **DEPRECATED** → `C.primaryBg` |

**Typography (T) — key composite roles:**
`T.h1` (28/35/700) · `T.h2` (22/29/700) · `T.h3` (18/25/700) · `T.body` (14/21/400) · `T.btn` (15/22/700) · `T.label` (12/17/700 uppercase) · `T.caption` (11/16/500)

**Hard design rules:**
- `fontWeight` max `'700'` — never `'800'` or `'900'` (T.black = '700')
- `shadowColor` → always `C.ink` (never raw `'#000'`)
- No emoji in UI text or push notifications — Ionicons only
- Audit command: `grep -rn "C\.green\b\|C\.greenBg\|fontWeight.*['\"8][0-9][0-9]['\"']\|shadowColor:.*'#" app/ components/`

### App Structure
*~800 tokens/session saved*
- Screens: `app/*.tsx`, `app/(tabs)/*.tsx`, `app/(provider)/*.tsx`. Edge Functions: `supabase/functions/*/index.ts` (stripe-webhook, create-payment-intent, release-escrow, cancel-contract). Shared logic: `lib/*.ts`.
- TypeScript check that exits clean: `npx tsc --noEmit 2>&1 | head -20`.

<!-- headroom:learn:start -->
## Headroom Learned Patterns
*Auto-generated by `headroom learn` on 2026-07-04 — do not edit manually*

### Session Handoff Files
*~2,500 tokens/session saved*
- Check the scratchpad directory FIRST for a staged `SESSION_HANDOFF.md` (e.g. `/tmp/claude-0/.../<session-id>/scratchpad/SESSION_HANDOFF.md`) before searching git — this session spent 6 tool calls (git status/log, ls, Glob, merge-base) before finding it staged there.
- If not in scratchpad and a Read 404s on `docs/SESSION_HANDOFF.md`, it may live on `origin/main` (seit `28e16cb`) — run `git fetch origin main` and read `git show origin/main:docs/SESSION_HANDOFF.md` (or fast-forward the branch) instead of hunting through Glob/`git log --all`.

### Iterative Screen Edits
*~1,200 tokens/session saved*
- When making many small changes to one screen file across a long session, read the file once, plan the full set of edits, then apply them in one pass — re-reading before every single Edit call wastes tokens (seen previously across `landing.tsx`, `suche.tsx`, `app/(tabs)/index.tsx`, `auftrag-aufgeben.tsx`).
- `Edit`/`Write` on a file not yet Read in this session throws `"File has not been read yet"` — Read immediately before the first Edit on any newly-touched file.
- For a multi-file feature pass (e.g. nachbarschaft feature touched `onboarding-kyc.tsx`, `nachbarschaft.tsx`, `nachbarschaft-profil.tsx`, `auftrag-detail.tsx` with 3-4 edits each), grep ALL edit-target locations — including StyleSheet/style-block insertion points — across every file up front; this session needed an extra mid-implementation grep+Read in `auftrag-detail.tsx` just to locate the StyleSheet block for the last edit.

### Headroom Tool Usage
*~400 tokens/session saved*
- `headroom learn --apply --target CLAUDE.md` (after `export PATH="$HOME/.local/bin:$PATH"`) applies learned patterns directly to CLAUDE.md — confirmed working command for closing the learn loop without manual copy-paste.

### Global Instructions / Karpathy Guidelines
*~1,500 tokens/session saved*
- The Karpathy guidelines content is injected into session context automatically (private global instructions from `/root/.claude/rules/ecc/common/karpathy-guidelines.md`). Don't re-read or re-download it via `curl` unless the user explicitly asks to persist it to disk; if the file is missing in a fresh container, that's cosmetic — the four binding rules stay versioned in AGENTS.md.

### Edge Functions
*~5,800 tokens/session saved*
- When doing a multi-concern pass across Edge Functions (idempotency, rate-limiting, input validation, PStTG thresholds, etc.), read and edit each `supabase/functions/<fn>/index.ts` file ONCE per file, applying all concerns in that single visit — not once per concern. This session re-read `create-payment-intent`, `stripe-webhook`, `cancel-contract`, and `release-escrow` 3x each (once per audit pass) at a combined cost of ~5,100 tokens in repeated Reads alone.
- Before starting a security/compliance sweep, list ALL public Edge Functions first (`ls supabase/functions/`) and plan the full set of changes per file, then touch each file exactly once.

### File Paths
*~2,425 tokens/session saved*
- Reading ANY `supabase/functions/*/index.ts` file (not just create-payment-intent/release-escrow) can return `runtime_error` while the full file content IS present in the response — treat as success and use the returned content rather than re-reading.
- `contexts/AuthContext.tsx` lives at project root; `supabase/functions/` may be absent from sparse worktrees (see Worktrees section).
- `supabase/functions/` directory is frequently absent from sparse worktrees even after sync. Fix: `cp -r /home/user/Ruflo/supabase/functions/ <worktree>/supabase/functions/` then Read each file before editing. Several `lib/` files (feeEngine.ts, auth.ts, contracts.ts) may also be absent — diagnose with `diff <(ls /home/user/Ruflo/lib/) <(ls <worktree>/lib/)`.

### Commands
*~1,500 tokens/session saved*
- Playwright: `PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers` is preset, but the launch executable is NOT at a guessed top-level path — run `find /opt/pw-browsers -iname "chrome" -o -iname "headless_shell"` first to get the exact binary path before writing a launch script, avoiding repeated failed launches (2 failed attempts, ~1,260 wasted tokens this session).
- `mcp__Google_Drive__search_files` with a broad `parentId` query can exceed the max-token result limit (85,962 chars seen) — pass a `pageSize` cap or scope the query further (e.g. add a `modifiedTime >` filter) before issuing a folder-wide search.
- Installing `headroom-ai`: use `pip install --user "headroom-ai[all]"` then `export PATH="$HOME/.local/bin:$PATH"` — the `headroom` binary is not on PATH by default after install.
- Quote all Bash paths containing parentheses: `ls "/path/app/(tabs)/"` not `ls /path/app/(tabs)/` — unquoted parens trigger a bash syntax error and abort the command. Also applies inside `for` loops.
- The Grep tool does NOT accept a `file_path` parameter — use `Bash: grep -n 'pattern' /path/to/file` for single-file grep instead.
- `find ... | xargs grep ...` returns exit code 123 when grep finds no matches (xargs propagates grep's exit code 1). Use `grep -rn 'pattern' <dir>` directly instead.

### Worktrees
*~3,000 tokens/session saved*
- Agents isolated in worktrees MUST edit files at `/home/user/Ruflo/.claude/worktrees/agent-XXXXXX/<file>`, NOT at `/home/user/Ruflo/<file>`. Edit/Write with the main repo path errors immediately — default first action in any worktree session: `git -C <worktree> reset --hard origin/claude/werkr-platform-build-64qstz` to sync ALL files before any reads or edits.
- When files are still missing after reset, three targeted fixes in order: (1) For specific files, run from the worktree CWD (no -C flag): `git checkout claude/werkr-platform-build-64qstz -- app/file.tsx "app/(tabs)/file.tsx"` (quote parenthesized paths). (2) For bulk missing files use merge: `git -C <worktree> fetch origin claude/werkr-platform-build-64qstz && git -C <worktree> merge origin/claude/werkr-platform-build-64qstz`. (3) Last resort: `cp /home/user/Ruflo/<path> <worktree>/<path>` then Read before Edit.
- `git checkout <branch>` fails inside a worktree when that branch is checked out in the main repo ("already used by worktree at ..."). Use `git -C <worktree-path> checkout <branch>` for worktree ops; `git -C /home/user/Ruflo ...` for main-repo ops.
- `git checkout -- <file>` (and `git restore`) with parenthesized paths inside a worktree requires quoting: `git -C <worktree> checkout -- "app/(provider)/file.tsx"`. Unquoted form fails with "pathspec did not match any file(s) known to git" even when the file exists.
- `supabase/functions/` directory is often absent in worktrees (not tracked in the worktree branch). Recovery: `mkdir -p <worktree>/supabase/functions/<fn>/ && cp /home/user/Ruflo/supabase/functions/<fn>/index.ts <worktree>/supabase/functions/<fn>/`, then Read before Edit.
- READ files from the worktree path in worktree sessions — do NOT read `/home/user/Ruflo/<file>` before editing. Reading the main repo version and then editing the worktree causes a wasted second Read (worktree content diverges as other agents commit). Verify availability with `ls <worktree>/app/` first, then read the worktree copy.
- The path is `.claude/worktrees/` — the recurring typo `.claire/worktrees/` appears in 5+ sessions and causes Edit/Write to return an isolation error pointing at the correct path. Copy the exact path from `ls /home/user/Ruflo/.claude/worktrees/` output.

### Large Files
*~4,000 tokens/session saved*
- `werkr-prototype.html` is ~270KB — exceeds both the 256KB Read size limit and the 25k-token Read token limit. Never attempt a full Read; 3+ sessions hit this error. Instead: `grep -n '^function ' werkr-prototype.html` to locate screen functions by line number, then `Read` with `offset` + `limit`. For navigation audits: `grep -n "go('" werkr-prototype.html | head -120`.
- `app/onboarding-kyc.tsx` is ~44KB — read with `offset`/`limit` when targeting specific sections.

### Database Schema
*~1,200 tokens/session saved*
- Schema now spans 19+ migrations (`001_initial_schema.sql` through `019_provider_profile_missing_fields.sql`). No single file has all tables — grep across all: `grep -rn 'create table' supabase/migrations/`.
- `contracts` and `offers` tables are in `002b_contracts_offers_tables.sql`, NOT in `001_initial_schema.sql`. Stripe/escrow fields (`stripe_payment_intent`, `stripe_account_id`, `escrow_captured_at`, `escrow_released_at`) are in later migrations, not `001`. `pstTg` compliance columns are in `012`. `disputes` table is in `013_disputes_table.sql`.
- `jobs` table gained `address_plz`, `address_city`, and `category` columns in `018_jobs_and_profiles_field_rename.sql`. `provider_profiles` got additional fields in `019_provider_profile_missing_fields.sql`.
- `reviews` table has NO `create table` migration — grep across all migration files returns empty. Do not search migrations for it; the table is not present as a SQL schema file.

### Tool Usage
*~500 tokens/session saved*
- Edit and Write always require a prior Read of the same file in the same agent session. For new files, confirm non-existence with `ls <path>` first, then Write. Skipping the Read causes a "File has not been read yet" error and a wasted round-trip.
- Read tool `offset` parameter must be a plain integer, never an array — use `offset: 500` not `offset: [500]`. Array form causes `InputValidationError: The parameter 'offset' type is expected as 'number' but provided as 'array'`.

### Design Tokens
*~500 tokens/session saved*
- Audit deprecated color/weight tokens in one grep: `grep -rn "C\.green\b\|C\.greenBg\|fontWeight.*['\"8][0-9][0-9]['\"']" app/ components/`. Also check hardcoded hex shadow colors: `grep -rn "shadowColor:.*'#\|shadowColor:.*\"#" app/ components/`. Both patterns confirmed clean means no remaining token drift.

### Agent Spawning
*~2,500 tokens/session saved*
- Audit subagents spawned via the `Agent` tool return 0 tool calls, 0 tokens, and 0.0s elapsed (silent no-op) — observed across 5 consecutive audit agents (AuditBatch1–4, AuditEdge). Do not delegate file-reading or audit tasks to subagents; read and audit files inline in the main session instead.

<!-- headroom:learn:end -->


## Headroom Learned Patterns
*Auto-generated by `headroom learn` on 2026-07-03 — do not edit manually*

### Iterative Screen Edits
*~20,000 tokens/session saved*
- When making many small changes to one screen file across a long session (this session hit 7-29 Reads/Edits each on `landing.tsx`, `suche.tsx`, `app/(tabs)/index.tsx`, `auftrag-aufgeben.tsx`), read the file once, plan the full set of edits, then apply them in one pass — re-reading before every single Edit call wasted ~19k tokens across just these 4 files this session.
- `Edit`/`Write` on a file not yet Read in this session throws `"File has not been read yet"` — this hit `supabase/config.toml`, `data/categories.ts`, `app/nachbarschaft.tsx`, `app/instant-preise.tsx`, `app/_layout.tsx` repeatedly; Read immediately before the first Edit on any newly-touched file, not just previously-known ones.

### GitHub Actions API
*~18,000 tokens/session saved*
- `mcp__github__actions_list` (`list_workflow_runs`) on this repo routinely returns 350k+ characters and throws a max-token error — this happened 6 times in one session before the agent worked around it. Skip the MCP tool for listing runs; go straight to `curl -s "https://api.github.com/repos/23mta23-cpu/Ruflo/actions/runs?per_page=5" | python3 -c "import json,sys; d=json.load(sys.stdin); [print(r['id'], r['status'], r['conclusion']) for r in d['workflow_runs'][:5]]"` for a compact status check.
- To confirm a Pages deploy landed, poll the bundle hash instead of the Actions API: `curl -s "https://23mta23-cpu.github.io/Ruflo/?v=$RANDOM" | grep -o '_expo/static/js/web/entry-[a-f0-9]*\.js'` and compare to the previous hash.

### Local Postgres Migration Testing
*~3,000 tokens/session saved*
- Docker is unavailable in this sandbox (`dockerd` won't start, no permissions) — use local Postgres instead: `service postgresql start`, then `su postgres -c "dropdb/createdb werkr_migration_test"`, then replay `supabase/migrations/*.sql` in numeric order from a copy directory.
- Any SQL file written to `/tmp` for `psql -f` must be `chmod 644` first, or `su postgres` gets `Permission denied` — this bit the session 3+ times (fix_auth_uid.sql, security_test.sql, idor_test.sql, rating_test.sql).
- `npx tsc --noEmit` does NOT typecheck `supabase/functions/` (outside the app's tsconfig scope). Use `deno check supabase/functions/<fn>/index.ts` instead — install with `curl -fsSL https://deno.land/install.sh | sh` then `export PATH="$HOME/.deno/bin:$PATH"`.

### GitHub Pages Deploy
*~2,500 tokens/session saved*
- `static.yml` is the sole workflow actually wired to GitHub Pages for this repo; `deploy-web.yml` was a duplicate/confusing path and was removed 2026-07-02. Don't debug both — check `static.yml` triggers/branches first.
- Pages deploys can take 5-7 minutes; a single `sleep 400-420 && curl ...` background check is the right pattern — don't re-poll more frequently.

### Playwright Screenshots of the App
*~2,500 tokens/session saved*
- Don't point Playwright at CDN-hosted scripts (e.g. jsdelivr React UMD builds) — the proxy blocks them (`ERR_CONNECTION_CLOSED`). For screenshotting the RN/Expo app, the working recipe is: `npx expo export --platform web --output-dir <scratch>/dist-X`, serve the output with a local `python3 -m http.server` (or a small SPA server script) on a free port, then screenshot `http://localhost:<port>/`.
- Chromium binary path is not at a top-level guess under `$PLAYWRIGHT_BROWSERS_PATH` — run `find /opt/pw-browsers -iname "chrome" -o -iname "headless_shell"` once and reuse that exact path in every launch script.

### Edge Functions
*~5,800 tokens/session saved*
- When doing a multi-concern pass across Edge Functions (idempotency, rate-limiting, input validation, PStTG thresholds, etc.), read and edit each `supabase/functions/<fn>/index.ts` file ONCE per file, applying all concerns in that single visit — not once per concern. This session re-read `create-payment-intent`, `stripe-webhook`, `cancel-contract`, and `release-escrow` 3x each (once per audit pass) at a combined cost of ~5,100 tokens in repeated Reads alone.
- Before starting a security/compliance sweep, list ALL public Edge Functions first (`ls supabase/functions/`) and plan the full set of changes per file, then touch each file exactly once.

### File Paths
*~2,425 tokens/session saved*
- Reading ANY `supabase/functions/*/index.ts` file (not just create-payment-intent/release-escrow) can return `runtime_error` while the full file content IS present in the response — treat as success and use the returned content rather than re-reading.
- `contexts/AuthContext.tsx` lives at project root; `supabase/functions/` may be absent from sparse worktrees (see Worktrees section).
- `supabase/functions/` directory is frequently absent from sparse worktrees even after sync. Fix: `cp -r /home/user/Ruflo/supabase/functions/ <worktree>/supabase/functions/` then Read each file before editing. Several `lib/` files (feeEngine.ts, auth.ts, contracts.ts) may also be absent — diagnose with `diff <(ls /home/user/Ruflo/lib/) <(ls <worktree>/lib/)`.

### Commands
*~1,500 tokens/session saved*
- Playwright: `PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers` is preset, but the launch executable is NOT at a guessed top-level path — run `find /opt/pw-browsers -iname "chrome" -o -iname "headless_shell"` first to get the exact binary path before writing a launch script, avoiding repeated failed launches (2 failed attempts, ~1,260 wasted tokens this session).
- `mcp__Google_Drive__search_files` with a broad `parentId` query can exceed the max-token result limit (85,962 chars seen) — pass a `pageSize` cap or scope the query further (e.g. add a `modifiedTime >` filter) before issuing a folder-wide search.
- Installing `headroom-ai`: use `pip install --user "headroom-ai[all]"` then `export PATH="$HOME/.local/bin:$PATH"` — the `headroom` binary is not on PATH by default after install.
- Quote all Bash paths containing parentheses: `ls "/path/app/(tabs)/"` not `ls /path/app/(tabs)/` — unquoted parens trigger a bash syntax error and abort the command. Also applies inside `for` loops.
- The Grep tool does NOT accept a `file_path` parameter — use `Bash: grep -n 'pattern' /path/to/file` for single-file grep instead.
- `find ... | xargs grep ...` returns exit code 123 when grep finds no matches (xargs propagates grep's exit code 1). Use `grep -rn 'pattern' <dir>` directly instead.

### Worktrees
*~3,000 tokens/session saved*
- Agents isolated in worktrees MUST edit files at `/home/user/Ruflo/.claude/worktrees/agent-XXXXXX/<file>`, NOT at `/home/user/Ruflo/<file>`. Edit/Write with the main repo path errors immediately — default first action in any worktree session: `git -C <worktree> reset --hard origin/claude/werkr-platform-build-64qstz` to sync ALL files before any reads or edits.
- When files are still missing after reset, three targeted fixes in order: (1) For specific files, run from the worktree CWD (no -C flag): `git checkout claude/werkr-platform-build-64qstz -- app/file.tsx "app/(tabs)/file.tsx"` (quote parenthesized paths). (2) For bulk missing files use merge: `git -C <worktree> fetch origin claude/werkr-platform-build-64qstz && git -C <worktree> merge origin/claude/werkr-platform-build-64qstz`. (3) Last resort: `cp /home/user/Ruflo/<path> <worktree>/<path>` then Read before Edit.
- `git checkout <branch>` fails inside a worktree when that branch is checked out in the main repo ("already used by worktree at ..."). Use `git -C <worktree-path> checkout <branch>` for worktree ops; `git -C /home/user/Ruflo ...` for main-repo ops.
- `git checkout -- <file>` (and `git restore`) with parenthesized paths inside a worktree requires quoting: `git -C <worktree> checkout -- "app/(provider)/file.tsx"`. Unquoted form fails with "pathspec did not match any file(s) known to git" even when the file exists.
- `supabase/functions/` directory is often absent in worktrees (not tracked in the worktree branch). Recovery: `mkdir -p <worktree>/supabase/functions/<fn>/ && cp /home/user/Ruflo/supabase/functions/<fn>/index.ts <worktree>/supabase/functions/<fn>/`, then Read before Edit.
- READ files from the worktree path in worktree sessions — do NOT read `/home/user/Ruflo/<file>` before editing. Reading the main repo version and then editing the worktree causes a wasted second Read (worktree content diverges as other agents commit). Verify availability with `ls <worktree>/app/` first, then read the worktree copy.
- The path is `.claude/worktrees/` — the recurring typo `.claire/worktrees/` appears in 5+ sessions and causes Edit/Write to return an isolation error pointing at the correct path. Copy the exact path from `ls /home/user/Ruflo/.claude/worktrees/` output.

### Large Files
*~4,000 tokens/session saved*
- `werkr-prototype.html` is ~270KB — exceeds both the 256KB Read size limit and the 25k-token Read token limit. Never attempt a full Read; 3+ sessions hit this error. Instead: `grep -n '^function ' werkr-prototype.html` to locate screen functions by line number, then `Read` with `offset` + `limit`. For navigation audits: `grep -n "go('" werkr-prototype.html | head -120`.
- `app/onboarding-kyc.tsx` is ~44KB — read with `offset`/`limit` when targeting specific sections.

### Database Schema
*~1,200 tokens/session saved*
- Schema now spans 19+ migrations (`001_initial_schema.sql` through `019_provider_profile_missing_fields.sql`). No single file has all tables — grep across all: `grep -rn 'create table' supabase/migrations/`.
- `contracts` and `offers` tables are in `002b_contracts_offers_tables.sql`, NOT in `001_initial_schema.sql`. Stripe/escrow fields (`stripe_payment_intent`, `stripe_account_id`, `escrow_captured_at`, `escrow_released_at`) are in later migrations, not `001`. `pstTg` compliance columns are in `012`. `disputes` table is in `013_disputes_table.sql`.
- `jobs` table gained `address_plz`, `address_city`, and `category` columns in `018_jobs_and_profiles_field_rename.sql`. `provider_profiles` got additional fields in `019_provider_profile_missing_fields.sql`.
- `reviews` table has NO `create table` migration — grep across all migration files returns empty. Do not search migrations for it; the table is not present as a SQL schema file.

### Tool Usage
*~500 tokens/session saved*
- Edit and Write always require a prior Read of the same file in the same agent session. For new files, confirm non-existence with `ls <path>` first, then Write. Skipping the Read causes a "File has not been read yet" error and a wasted round-trip.
- Read tool `offset` parameter must be a plain integer, never an array — use `offset: 500` not `offset: [500]`. Array form causes `InputValidationError: The parameter 'offset' type is expected as 'number' but provided as 'array'`.

### Design Tokens
*~500 tokens/session saved*
- Audit deprecated color/weight tokens in one grep: `grep -rn "C\.green\b\|C\.greenBg\|fontWeight.*['\"8][0-9][0-9]['\"']" app/ components/`. Also check hardcoded hex shadow colors: `grep -rn "shadowColor:.*'#\|shadowColor:.*\"#" app/ components/`. Both patterns confirmed clean means no remaining token drift.

### Agent Spawning
*~2,500 tokens/session saved*
- Audit subagents spawned via the `Agent` tool return 0 tool calls, 0 tokens, and 0.0s elapsed (silent no-op) — observed across 5 consecutive audit agents (AuditBatch1–4, AuditEdge). Do not delegate file-reading or audit tasks to subagents; read and audit files inline in the main session instead.
